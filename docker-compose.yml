services:
  db:
    image: pgvector/pgvector:pg17
    container_name: vocesindigenas-db
    restart: unless-stopped
    ports:
      - "5433:5432"
    env_file:
      - .env.docker
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
