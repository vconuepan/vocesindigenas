[extend]
useDefault = true

[[allowlists]]
description = "Sample/placeholder values in .env.sample and docs"
regexes = [
  'sk-\.\.\.',
  'sk_\.\.\.',
  'your_token_here',
  'changeme',
  'change_me_locally',
  'your_',
  'example\.',
  '\[at least',
  'pk_\.\.\.',
]
paths = [
  'server/.env.sample',
  '\.md$',
]

[[allowlists]]
description = "Render placeholder DATABASE_URL in CI"
regexes = [
  'postgresql://user:password@localhost',
]
paths = [
  '\.github/workflows/',
]

[[allowlists]]
description = "Test files — fake secrets in fixtures and mocks"
paths = [
  '\.test\.ts$',
  '\.spec\.ts$',
]

[[allowlists]]
description = "Compiled output and dependencies — not source of truth"
paths = [
  'dist/',
  'node_modules/',
]
